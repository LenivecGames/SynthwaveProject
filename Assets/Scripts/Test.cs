using UnityEngine;
using System.Collections;

[ExecuteInEditMode]
public class Test : MonoBehaviour
{
    //public Vector3 worldToScreen;
    //public Vector3 screenToWorld;
    // Use this for initialization
    void Start()
    {
        /*print("Standard " + transform.position.x);//0

        worldToScreen = Camera.main.WorldToScreenPoint(transform.position);
        print("World " + worldToScreen.x); //600

        screenToWorld = Camera.main.ScreenToWorldPoint(worldToScreen);
        print("Screen " + screenToWorld.x); //0*/
    }

    // Update is called once per frame
    void Update()
    {
       /* worldToScreen = Camera.main.WorldToScreenPoint(transform.position);

        screenToWorld = Camera.main.ScreenToWorldPoint(worldToScreen);*/
    }
}
